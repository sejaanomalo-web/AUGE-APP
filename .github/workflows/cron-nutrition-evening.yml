name: cron nutrition-evening

on:
  schedule:
    - cron: "0 22 * * *"
  workflow_dispatch:

concurrency:
  group: cron-nutrition-evening
  cancel-in-progress: false

jobs:
  fire:
    runs-on: ubuntu-latest
    steps:
      - name: Hit /api/cron/nutrition-evening
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          ENDPOINT: ${{ vars.CRON_BASE_URL || 'https://auge-app.vercel.app' }}/api/cron/nutrition-evening
        run: |
          curl -fsSL --retry 3 --retry-delay 10 --max-time 120 \
            -H "Authorization: Bearer ${CRON_SECRET}" \
            "${ENDPOINT}"
